{
  "name": "notesbackend",
  "version": "1.0.0",
  "description": "part3 exercises",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phone-agenda && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/Dixser/notesBackend.git"
  },
  "author": "Dani Ortiz",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Dixser/notesBackend/issues"
  },
  "homepage": "https://github.com/Dixser/notesBackend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongodb": "^6.9.0",
    "mongoose": "^8.7.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
